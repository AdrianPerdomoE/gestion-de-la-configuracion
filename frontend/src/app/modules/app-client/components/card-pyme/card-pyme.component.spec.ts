/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CardPymeComponent } from './card-pyme.component';

describe('CardPymeComponent', () => {
  let component: CardPymeComponent;
  let fixture: ComponentFixture<CardPymeComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ CardPymeComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(CardPymeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/