/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ProductPymeViewComponent } from './product-pyme-view.component';

describe('ProductPymeViewComponent', () => {
  let component: ProductPymeViewComponent;
  let fixture: ComponentFixture<ProductPymeViewComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ProductPymeViewComponent ]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ProductPymeViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/